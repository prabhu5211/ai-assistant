PORT=3001
LLM_API_KEY=your_gemini_api_key_here
LLM_PROVIDER=mock
# Options: mock (no API key needed), gemini (FREE), openai

# For Mock (testing): No API key needed, uses simple keyword matching
# For Gemini (FREE): Get key from https://aistudio.google.com/app/apikey
# For OpenAI: Get key from https://platform.openai.com/api-keys
